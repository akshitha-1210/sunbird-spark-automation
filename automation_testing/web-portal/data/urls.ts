const base = process.env.BASE_URL ?? 'https://sandbox.sunbirded.org';

export const urls = {
  main: base,
  home: `${base}/home`,
  explore: `${base}/explore`,
  myLearning: `${base}/my-learning`,
  profile: `${base}/profile`,
};
