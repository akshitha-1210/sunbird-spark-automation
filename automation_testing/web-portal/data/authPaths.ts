import path from 'path';

export const authPaths = {
  googleUser: path.join('.auth', 'googleUser.json'),
  registeredUser: path.join('.auth', 'registeredUser.json'),
  user2: path.join('.auth', 'user2.json'),
};
